# Fork deviations

GENERATED by `uv run deviations render` -- do not edit.

Source of truth: `benchmark/methods/methods.yaml + git log upstream-base..HEAD in each fork`.

- manifest version: 1
- audit source: `resources/obsidian/benchmarking/surveys/fork-audit.md` (2026-09-05)

A measurement may not be produced from code whose deviations are not
recorded (CLAUDE.md, "Deviation policy"). `git log upstream-base..HEAD` in
a fork is its exhaustive deviation list; this file is that log plus the
deviations that live in the harness rather than in a fork.

## Summary

| method           | provenance               | status   | commits | bugfix | build | compat | instrumentation | interface | normalization |
|------------------|--------------------------|----------|---------|--------|-------|--------|-----------------|-----------|---------------|
| ACMH             | published-reference      | active   | 8       |        | 3     | 2      | 2               | 1         |               |
| ACMM             | published-reference      | active   | 7       |        | 3     | 1      | 2               | 1         |               |
| ACMP             | published-reference      | active   | 8       |        | 3     | 1      | 2               | 1         | 1             |
| ACMMP            | published-reference      | active   | 9       |        | 3     | 2      | 2               | 1         | 1             |
| APD-MVS          | published-reference      | active   | 13      |        | 3     | 3      | 2               | 4         | 1             |
| HPM-MVS          | published-reference      | active   | 13      | 2      | 4     | 4      | 1               | 1         | 1             |
| HPM-MVS_plusplus | published-reference      | active   | 13      | 3      | 3     | 4      | 1               | 1         | 1             |
| MP-MVS           | published-reference      | active   | 16      | 2      | 4     | 5      | 2               | 2         | 1             |
| DPE-MVS          | published-reference      | active   | 10      |        | 3     | 2      | 2               | 2         | 1             |
| DVP-MVS          | published-reference      | excluded | 0       |        |       |        |                 |           |               |
| CUMVS            | third-party-optimization | active   | 3       |        | 1     |        | 1               | 1         |               |

Provenance is load-bearing: `third-party-optimization` is prior art by a
third party, not the thesis author's work.

## ACMH

- provenance: `published-reference`
- status: `active`
- path: `benchmark/methods/ACMH`
- upstream: https://github.com/GhiXu/ACMH
- upstream_base: `7db8c1b21af1dc5990fda066723e87c7e9e59a55`
- fork HEAD: `048d479cb2a778caac2adfdb4acf260c9279af74`

### Commits in `upstream-base..HEAD`

| sha          | class           | affects               | reversible                                                  | summary                                                                            |
|--------------|-----------------|-----------------------|-------------------------------------------------------------|------------------------------------------------------------------------------------|
| 63eca94101a9 | build           | timing,memory,quality | no                                                          | build: modernise CMake and raise the language standard to C++20                    |
| 565c2992a0f7 | build           | timing,memory         | no                                                          | build: request only the OpenCV components ACMH uses                                |
| 2fb70fadaf12 | interface       | none                  | no                                                          | interface: ignore build output and editor state                                    |
| 0457ccad7e6a | build           | timing,memory,quality | no                                                          | build: build CUDA through the CUDA language so the target architecture is honoured |
| 8ba676cff99b | compat          | none                  | no                                                          | compat: replace np.asscalar with ndarray.item in the shipped converter             |
| ec7d0e1540f5 | compat          | none                  | no                                                          | compat: select CUDA device 0 instead of device 1                                   |
| bccf6d6c221b | instrumentation | timing                | flag -DBENCH_PHASES=0 (compile), env MVS_BENCH_PHASES (run) | instrumentation: emit phase boundaries through bench_timer.h                       |
| 048d479cb2a7 | instrumentation | timing                | flag -DBENCH_PHASES=0 (compile), env MVS_BENCH_PHASES (run) | instrumentation: allow a phase span to be closed before its scope ends             |

### Harness deviations

| class         | affects               | reversible           | description                                                                                                                                                                           |
|---------------|-----------------------|----------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| normalization | quality               | configuration author | norm10 only: shared colmap2mvsnet_acm_perf instead of the fork's converter; author runs the fork's own.                                                                               |
| normalization | timing,memory,quality | flag --neighbours=20 | norm10: converter emits 10 source views instead of the shipped 20.                                                                                                                    |
| normalization | none                  |                      | R-EXP-11 (debug artefact output off) needs no action for this method: it writes no debug artefacts. The harness must NOT pass --no-debug-output to it -- the binary has no such flag. |

## ACMM

- provenance: `published-reference`
- status: `active`
- path: `benchmark/methods/ACMM`
- upstream: https://github.com/GhiXu/ACMM
- upstream_base: `e7f34fa56931e026078e4f11bdf53f54dddc9ccc`
- fork HEAD: `8ec132477725181bed29273f053f3b66aa8b20a9`

### Commits in `upstream-base..HEAD`

| sha          | class           | affects               | reversible                                                  | summary                                                                            |
|--------------|-----------------|-----------------------|-------------------------------------------------------------|------------------------------------------------------------------------------------|
| ae0cd1fa861b | build           | timing,memory,quality | no                                                          | build: modernise CMake and raise the language standard to C++20                    |
| dabf1fe158ca | build           | timing,memory         | no                                                          | build: request only the OpenCV components ACMM uses                                |
| 7fc2320855c7 | interface       | none                  | no                                                          | interface: ignore build output and editor state                                    |
| 3fa458a7baa3 | build           | timing,memory,quality | no                                                          | build: build CUDA through the CUDA language so the target architecture is honoured |
| dcae6e11a27f | compat          | none                  | no                                                          | compat: replace np.asscalar with ndarray.item in the shipped converter             |
| ff3b722727ff | instrumentation | timing                | flag -DBENCH_PHASES=0 (compile), env MVS_BENCH_PHASES (run) | instrumentation: emit phase boundaries through bench_timer.h                       |
| 8ec132477725 | instrumentation | timing                | flag -DBENCH_PHASES=0 (compile), env MVS_BENCH_PHASES (run) | instrumentation: allow a phase span to be closed before its scope ends             |

### Harness deviations

| class         | affects               | reversible           | description                                                                                                                                                                           |
|---------------|-----------------------|----------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| normalization | quality               | configuration author | norm10 only: shared colmap2mvsnet_acm_perf instead of the fork's converter; author runs the fork's own.                                                                               |
| normalization | timing,memory,quality | flag --neighbours=20 | norm10: converter emits 10 source views instead of the shipped 20.                                                                                                                    |
| normalization | none                  |                      | R-EXP-11 (debug artefact output off) needs no action for this method: it writes no debug artefacts. The harness must NOT pass --no-debug-output to it -- the binary has no such flag. |

## ACMP

- provenance: `published-reference`
- status: `active`
- path: `benchmark/methods/ACMP`
- upstream: https://github.com/GhiXu/ACMP
- upstream_base: `574c8e078b6f7bd93237bd180d46f5adf1169a19`
- fork HEAD: `0837cc664ad749bd47a7414007411d22cc7715ba`

### Commits in `upstream-base..HEAD`

| sha          | class           | affects               | reversible                                                  | summary                                                                            |
|--------------|-----------------|-----------------------|-------------------------------------------------------------|------------------------------------------------------------------------------------|
| ce70eedbb80a | build           | timing,memory,quality | no                                                          | build: modernise CMake and raise the language standard to C++20                    |
| ea3a014b0ded | build           | timing,memory         | no                                                          | build: request only the OpenCV components ACMP uses                                |
| b39892be6d70 | interface       | none                  | no                                                          | interface: ignore build output and editor state                                    |
| e5843610e456 | build           | timing,memory,quality | no                                                          | build: build CUDA through the CUDA language so the target architecture is honoured |
| e93424d171d0 | compat          | none                  | no                                                          | compat: replace np.asscalar with ndarray.item in the shipped converter             |
| b58da296694b | instrumentation | timing                | flag -DBENCH_PHASES=0 (compile), env MVS_BENCH_PHASES (run) | instrumentation: emit phase boundaries through bench_timer.h                       |
| 8ade8ced47a9 | normalization   | timing,io             | flag --no-debug-output                                      | normalization: add --no-debug-output to suppress the triangulation render          |
| 0837cc664ad7 | instrumentation | timing                | flag -DBENCH_PHASES=0 (compile), env MVS_BENCH_PHASES (run) | instrumentation: allow a phase span to be closed before its scope ends             |

### Harness deviations

| class         | affects               | reversible             | description                                                                                                                                                                                         |
|---------------|-----------------------|------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| normalization | quality               | configuration author   | norm10 only: shared colmap2mvsnet_acm_perf instead of the fork's converter; author runs the fork's own.                                                                                             |
| normalization | timing,memory,quality | flag --neighbours=20   | norm10: converter emits 10 source views instead of the shipped 20.                                                                                                                                  |
| normalization | timing,io             | flag --no-debug-output | Debug artefact output disabled in both configurations by passing --no-debug-output (R-EXP-11). The fork's own default is the upstream behaviour; see global_deviations for what each method writes. |

## ACMMP

- provenance: `published-reference`
- status: `active`
- path: `benchmark/methods/ACMMP`
- upstream: https://github.com/GhiXu/ACMMP
- upstream_base: `5b4c5fede90c301d37853d3ea8943fd9e93dcce8`
- fork HEAD: `aa6e3cf0f440eed1b881de8917d80784d0a12c8b`

### Commits in `upstream-base..HEAD`

| sha          | class           | affects               | reversible                                                  | summary                                                                            |
|--------------|-----------------|-----------------------|-------------------------------------------------------------|------------------------------------------------------------------------------------|
| 4ab83e92fabd | compat          | none                  | no                                                          | compat: drop the hardcoded CUDA 8.0 toolkit path                                   |
| 9c0265291881 | build           | timing,memory,quality | no                                                          | build: modernise CMake and raise the language standard to C++20                    |
| 8909587ec76b | build           | timing,memory         | no                                                          | build: request only the OpenCV components ACMMP uses                               |
| 8e0faf337486 | interface       | none                  | no                                                          | interface: ignore build output and editor state                                    |
| a1a9e7c6d739 | build           | timing,memory,quality | no                                                          | build: build CUDA through the CUDA language so the target architecture is honoured |
| 0a3462727a47 | compat          | none                  | no                                                          | compat: replace np.asscalar with ndarray.item in the shipped converter             |
| 7f62cece70ba | instrumentation | timing                | flag -DBENCH_PHASES=0 (compile), env MVS_BENCH_PHASES (run) | instrumentation: emit phase boundaries through bench_timer.h                       |
| 2d34707ba16b | normalization   | timing,io             | flag --no-debug-output                                      | normalization: add --no-debug-output to suppress the triangulation render          |
| aa6e3cf0f440 | instrumentation | timing                | flag -DBENCH_PHASES=0 (compile), env MVS_BENCH_PHASES (run) | instrumentation: allow a phase span to be closed before its scope ends             |

### Harness deviations

| class         | affects               | reversible             | description                                                                                                                                                                                         |
|---------------|-----------------------|------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| normalization | quality               | configuration author   | norm10 only: shared colmap2mvsnet_acm_perf instead of the fork's converter; author runs the fork's own.                                                                                             |
| normalization | timing,memory,quality | flag --neighbours=20   | norm10: converter emits 10 source views instead of the shipped 20.                                                                                                                                  |
| normalization | timing,io             | flag --no-debug-output | Debug artefact output disabled in both configurations by passing --no-debug-output (R-EXP-11). The fork's own default is the upstream behaviour; see global_deviations for what each method writes. |

## APD-MVS

- provenance: `published-reference`
- status: `active`
- path: `benchmark/methods/APD-MVS`
- upstream: https://github.com/whoiszzj/APD-MVS
- upstream_base: `95be7c9bd267969cf11f906367ab56d1f5f85a59`
- fork HEAD: `b105b0b202d81ea2f50f9033196c6ddc93ff8982`

### Commits in `upstream-base..HEAD`

| sha          | class           | affects               | reversible                                                  | summary                                                                                 |
|--------------|-----------------|-----------------------|-------------------------------------------------------------|-----------------------------------------------------------------------------------------|
| 318ce7614b74 | compat          | none                  | no                                                          | compat: replace Boost.Filesystem with the standard <filesystem>                         |
| a97442a1f87b | compat          | none                  | no                                                          | compat: include the CUDA runtime headers where they are actually used                   |
| 9b8570cf8c57 | build           | timing,memory,quality | no                                                          | build: build CUDA through the CUDA language, drop Boost and raise the standard to C++20 |
| c3e25a19c8af | build           | timing,memory         | no                                                          | build: request only the OpenCV components APD-MVS uses                                  |
| c8c34580889f | interface       | io                    | no                                                          | interface: rename the PLY colour properties to the standard names                       |
| 6aad232be069 | interface       | none                  | no                                                          | interface: say which image dimension mismatched in CheckImages                          |
| 77d428097c0c | interface       | none                  | no                                                          | interface: ignore build output and editor state                                         |
| b5b51835ef2c | build           | timing,memory,quality | no                                                          | build: stop compiling device code as relocatable device code                            |
| 3d7812d1a0c8 | compat          | none                  | no                                                          | compat: replace np.asscalar with ndarray.item in the shipped converter                  |
| ff77640c13be | instrumentation | timing                | flag -DBENCH_PHASES=0 (compile), env MVS_BENCH_PHASES (run) | instrumentation: emit phase boundaries through bench_timer.h                            |
| ed3185a017bb | normalization   | timing,io             | flag --no-debug-output                                      | normalization: add --no-debug-output to suppress the per-pass debug JPEGs               |
| 3cda90f5804d | instrumentation | timing                | flag -DBENCH_PHASES=0 (compile), env MVS_BENCH_PHASES (run) | instrumentation: allow a phase span to be closed before its scope ends                  |
| b105b0b202d8 | interface       | io                    | no                                                          | interface: write the PLY colour channels in red-green-blue order                        |

### Harness deviations

| class         | affects               | reversible             | description                                                                                                                                                                                                                            |
|---------------|-----------------------|------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| normalization | quality               | configuration author   | norm10 only: shared colmap2mvsnet_acm_perf (with --padding if the scene needs it, then for every method) instead of the fork's own converter, which pads AND re-encodes AND optionally rescales. author runs the fork's own converter. |
| normalization | quality,timing        | flag --padding         | Zero-padding of every image to the scene maximum, required by CheckImages(). Applied to this method only, so its input pixels differ from the non-padding methods' - a disclosed asymmetry, not an equalisation.                       |
| normalization | timing,memory,quality | flag --neighbours=20   | norm10: converter emits 10 source views instead of the shipped 20.                                                                                                                                                                     |
| normalization | timing,io             | flag --no-debug-output | Debug artefact output disabled in both configurations by passing --no-debug-output (R-EXP-11). The fork's own default is the upstream behaviour; see global_deviations for what each method writes.                                    |

## HPM-MVS

- provenance: `published-reference`
- status: `active`
- path: `benchmark/methods/HPM-MVS`
- upstream: https://github.com/CLinvx/HPM-MVS
- upstream_base: `a4f951e657c6ba6ded8fdb17bdcd064f6e5fc5cf`
- fork HEAD: `190d36ac3a71e473d87e3113c0f063cafa6e680d`

### Commits in `upstream-base..HEAD`

| sha          | class           | affects               | reversible                                                  | summary                                                                     |
|--------------|-----------------|-----------------------|-------------------------------------------------------------|-----------------------------------------------------------------------------|
| 59d1dcbf73af | compat          | none                  | no                                                          | compat: drop the MSVC-only <direct.h> include                               |
| 774e88227a12 | compat          | none                  | no                                                          | compat: create result directories with std::filesystem                      |
| 0e85a9d11e05 | compat          | memory                | no                                                          | compat: release image vectors by assignment instead of swap-with-temporary  |
| c4db1753e6c8 | bugfix          | none                  | no                                                          | bugfix: write the depth to point3D[2] instead of one past the end           |
| bc10dc348227 | bugfix          | none                  | no                                                          | bugfix: release new[] allocations with delete[]                             |
| 67a40c64e308 | build           | timing,memory,quality | no                                                          | build: build CUDA through the CUDA language and raise the standard to C++20 |
| 083966a9d215 | build           | timing,memory         | no                                                          | build: drop the unused PCL visualisation headers                            |
| 2060f960b407 | build           | timing,memory         | no                                                          | build: request only the OpenCV components HPM-MVS uses                      |
| a3ba298f027d | interface       | none                  | no                                                          | interface: ignore build output and editor state                             |
| 4b08253927d1 | build           | timing,memory,quality | no                                                          | build: stop compiling device code as relocatable device code                |
| 972c7a1023ff | instrumentation | timing                | flag -DBENCH_PHASES=0 (compile), env MVS_BENCH_PHASES (run) | instrumentation: emit phase boundaries through bench_timer.h                |
| bdfd4e4540e4 | normalization   | timing,io             | flag --no-debug-output                                      | normalization: add --no-debug-output to suppress the prior depth map dump   |
| 190d36ac3a71 | compat          | none                  | no                                                          | compat: replace np.asscalar with ndarray.item in the shipped converter      |

### Harness deviations

| class         | affects               | reversible             | description                                                                                                                                                                                         |
|---------------|-----------------------|------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| normalization | quality               | configuration author   | norm10 only: shared colmap2mvsnet_acm_perf instead of the fork's converter; author runs the fork's own.                                                                                             |
| normalization | timing,memory,quality | flag --neighbours=20   | norm10: converter emits 10 source views instead of the shipped 20.                                                                                                                                  |
| normalization | timing,io             | flag --no-debug-output | Debug artefact output disabled in both configurations by passing --no-debug-output (R-EXP-11). The fork's own default is the upstream behaviour; see global_deviations for what each method writes. |

## HPM-MVS_plusplus

- provenance: `published-reference`
- status: `active`
- path: `benchmark/methods/HPM-MVS_plusplus`
- upstream: https://github.com/CLinvx/HPM-MVS_plusplus
- upstream_base: `946bebdba367c567a4b64220d4d7e8622cdc3a05`
- fork HEAD: `1ad8aa7cd887e2d0e5fa44cedf80768a807fabf9`

### Commits in `upstream-base..HEAD`

| sha          | class           | affects               | reversible                                                  | summary                                                                     |
|--------------|-----------------|-----------------------|-------------------------------------------------------------|-----------------------------------------------------------------------------|
| a40a50ba78b7 | compat          | none                  | no                                                          | compat: drop <direct.h> and create result directories with std::filesystem  |
| 0a2894dafe32 | compat          | quality               | no                                                          | compat: read confidence.dmb under the name the producer writes              |
| 10d4eb5b6035 | bugfix          | none                  | no                                                          | bugfix: write the depth to point3D[2] instead of one past the end           |
| 8ee4e1cf8678 | compat          | memory                | no                                                          | compat: release image vectors by assignment instead of swap-with-temporary  |
| aadbad37ef82 | bugfix          | none                  | no                                                          | bugfix: release new[] allocations with delete[]                             |
| 68a07c96c067 | bugfix          | none                  | no                                                          | bugfix: make the mask argument actually optional                            |
| 390b93537cd2 | build           | timing,memory,quality | no                                                          | build: build CUDA through the CUDA language and raise the standard to C++20 |
| 3aa7cbc1e129 | build           | timing,memory         | no                                                          | build: request only the OpenCV components HPM-MVS_plusplus uses             |
| 2fe4864aae16 | interface       | none                  | no                                                          | interface: ignore build output and editor state                             |
| e46a87d87b01 | build           | timing,memory,quality | no                                                          | build: stop compiling device code as relocatable device code                |
| 4eea9f71d837 | compat          | none                  | no                                                          | compat: replace np.asscalar with ndarray.item in the shipped converter      |
| 3ed2788c4492 | instrumentation | timing                | flag -DBENCH_PHASES=0 (compile), env MVS_BENCH_PHASES (run) | instrumentation: emit phase boundaries through bench_timer.h                |
| 1ad8aa7cd887 | normalization   | timing,io             | flag --no-debug-output                                      | normalization: add --no-debug-output to suppress the prior debug artefacts  |

### Harness deviations

| class         | affects               | reversible             | description                                                                                                                                                                                         |
|---------------|-----------------------|------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| normalization | quality               | configuration author   | norm10 only: shared colmap2mvsnet_acm_perf instead of the fork's converter; author runs the fork's own.                                                                                             |
| normalization | timing,memory,quality | flag --neighbours=20   | norm10: converter emits 10 source views instead of the shipped 20.                                                                                                                                  |
| normalization | timing,io             | flag --no-debug-output | Debug artefact output disabled in both configurations by passing --no-debug-output (R-EXP-11). The fork's own default is the upstream behaviour; see global_deviations for what each method writes. |

## MP-MVS

- provenance: `published-reference`
- status: `active`
- path: `benchmark/methods/MP-MVS`
- upstream: https://github.com/RongxuanTan/MP-MVS
- upstream_base: `17e3c05e9706ce9735e466756492f9967238afff`
- fork HEAD: `0f792f27241498cb5d5fdaa3055f8c9bdc4301e8`

### Commits in `upstream-base..HEAD`

| sha          | class           | affects               | reversible                                                  | summary                                                                               |
|--------------|-----------------|-----------------------|-------------------------------------------------------------|---------------------------------------------------------------------------------------|
| 0b383ef27651 | interface       | none                  | no                                                          | interface: ignore build output and editor state, and drop the committed .idea project |
| d56fb562f295 | compat          | none                  | no                                                          | compat: include <fstream> where the file streams are used                             |
| 866f6c06942d | compat          | none                  | no                                                          | compat: discover CUDA through the toolchain instead of a hardcoded 11.0 path          |
| 257b993f52db | compat          | none                  | no                                                          | compat: remove the generated ProjectPath.h and its hardcoded /home/xuan/MP-MVS        |
| 59c614d4c35e | interface       | io                    | no                                                          | interface: take the dataset folder and the config file from the command line          |
| e168d16df6a7 | compat          | io                    | no                                                          | compat: write the debug depth maps under the output folder                            |
| 9b90250b69f3 | build           | timing,memory         | no                                                          | build: stop forcing a Debug build                                                     |
| d0a3091e7d7c | build           | timing,memory,quality | no                                                          | build: build CUDA through the CUDA language so the nvcc flags are honoured            |
| bbc5e9544b02 | build           | timing,memory,quality | no                                                          | build: raise the language standard to C++20                                           |
| 7aae4ed10d0e | build           | timing,memory         | no                                                          | build: request only the OpenCV components MP-MVS uses                                 |
| 782af68c80e6 | bugfix          | none                  | no                                                          | bugfix: create the parent directory before writing a depth map                        |
| f156027cd1cf | bugfix          | none                  | no                                                          | bugfix: return a value from SaveDmb                                                   |
| fe7c75d3fc7f | compat          | none                  | no                                                          | compat: replace np.asscalar with ndarray.item in the shipped converter                |
| cb586dae8b5e | instrumentation | timing                | flag -DBENCH_PHASES=0 (compile), env MVS_BENCH_PHASES (run) | instrumentation: emit phase boundaries through bench_timer.h                          |
| 399d137f2216 | normalization   | timing,io             | flag --no-debug-output                                      | normalization: add --no-debug-output to suppress the JPEG debug output                |
| 0f792f272414 | instrumentation | timing                | flag -DBENCH_PHASES=0 (compile), env MVS_BENCH_PHASES (run) | instrumentation: drop the scene_scan span around the config read                      |

### Harness deviations

| class         | affects               | reversible             | description                                                                                                                                                                                                                                                |
|---------------|-----------------------|------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| normalization | quality               | configuration author   | norm10 only: shared colmap2mvsnet_acm_perf instead of the fork's converter; author runs the fork's own.                                                                                                                                                    |
| normalization | timing,memory,quality | flag --neighbours=20   | norm10: converter emits 10 source views instead of the shipped 20.                                                                                                                                                                                         |
| interface     | none                  |                        | The harness supplies the config file location as the second argument ({config} -> config_path). Recorded here because the choice of which config file a run uses lives in the harness, not in the fork, and the parameters in it belong in the run record. |
| normalization | timing,io             | flag --no-debug-output | Debug artefact output disabled in both configurations by passing --no-debug-output (R-EXP-11). The fork's own default is the upstream behaviour; see global_deviations for what each method writes.                                                        |

## DPE-MVS

- provenance: `published-reference`
- status: `active`
- path: `benchmark/methods/DPE-MVS`
- upstream: https://github.com/M3phist0/DPE-MVS
- upstream_base: `e1ff7e3db12d83b2b8685beef1055b6de5801505`
- fork HEAD: `5f5a8bacfc912acac42a62c020e3269079ea3be2`

### Commits in `upstream-base..HEAD`

| sha          | class           | affects               | reversible                                                  | summary                                                                                 |
|--------------|-----------------|-----------------------|-------------------------------------------------------------|-----------------------------------------------------------------------------------------|
| c14e6d880840 | compat          | timing,memory,quality | no                                                          | compat: stop pinning the C and C++ compilers to /usr/bin/gcc                            |
| fc0bba4ee80b | compat          | none                  | no                                                          | compat: replace Boost.Filesystem with the standard <filesystem>                         |
| e1e11c1acfa0 | build           | timing,memory,quality | no                                                          | build: build CUDA through the CUDA language, drop Boost and raise the standard to C++20 |
| b51cd7ee5014 | build           | timing,memory         | no                                                          | build: request only the OpenCV components DPE-MVS uses                                  |
| a33319da71d1 | interface       | io                    | no                                                          | interface: rename the PLY colour properties to the standard names                       |
| 1f0de8713a84 | interface       | none                  | no                                                          | interface: ignore build output and editor state                                         |
| 6959e960a65f | build           | timing,memory,quality | no                                                          | build: stop compiling device code as relocatable device code                            |
| 2bd5e48759ef | instrumentation | timing                | flag -DBENCH_PHASES=0 (compile), env MVS_BENCH_PHASES (run) | instrumentation: emit phase boundaries through bench_timer.h                            |
| e25b80c3951a | normalization   | timing,io             | flag --no-debug-output                                      | normalization: add --no-debug-output to suppress the debug image writes                 |
| 5f5a8bacfc91 | instrumentation | timing                | flag -DBENCH_PHASES=0 (compile), env MVS_BENCH_PHASES (run) | instrumentation: allow a phase span to be closed before its scope ends                  |

### Harness deviations

| class         | affects               | reversible             | description                                                                                                                                                                                                                            |
|---------------|-----------------------|------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| normalization | quality               | configuration author   | norm10 only: shared colmap2mvsnet_acm_perf (with --padding if the scene needs it, then for every method) instead of the fork's own converter, which pads AND re-encodes AND optionally rescales. author runs the fork's own converter. |
| normalization | quality,timing        | flag --padding         | Zero-padding of every image to the scene maximum, required by CheckImages().                                                                                                                                                           |
| normalization | timing,memory,quality | flag --neighbours=20   | norm10: converter emits 10 source views instead of the shipped 20.                                                                                                                                                                     |
| normalization | timing,io             | flag --no-debug-output | Debug artefact output disabled in both configurations by passing --no-debug-output (R-EXP-11). The fork's own default is the upstream behaviour; see global_deviations for what each method writes.                                    |

## DVP-MVS

- provenance: `published-reference`
- status: `excluded`
- path: `benchmark/methods/DVP-MVS`
- upstream: https://github.com/ZhenlongYuan/DVP-MVS
- upstream_base: `5b2b31c02941cfa28808b4e37f06a87e8dc0f2d9`
- fork HEAD: `5b2b31c02941cfa28808b4e37f06a87e8dc0f2d9`

No commits after `upstream-base`: this fork is excluded from the
campaign and is kept pristine; `deviations verify` enforces that.

## CUMVS

- provenance: `third-party-optimization`
- status: `active`
- path: `benchmark/methods/cuda-multi-view-stereo`
- upstream: https://github.com/fixstars/cuda-multi-view-stereo
- upstream_base: `2efda9410ced0806b8f0ee3735cd5f14a6d4c99f`
- fork HEAD: `cff89eb6c74cf37f05d5ea6aade55cc68e590007`

### Commits in `upstream-base..HEAD`

| sha          | class           | affects | reversible                                                                     | summary                                                      |
|--------------|-----------------|---------|--------------------------------------------------------------------------------|--------------------------------------------------------------|
| 34e9a01e9567 | build           | none    | no                                                                             | build: find VTK, Qt5 and Eigen3 explicitly                   |
| 72218adb5870 | interface       | none    | no                                                                             | interface: ignore build output and editor state              |
| cff89eb6c74c | instrumentation | timing  | flag -DBENCH_PHASES=0 (compile), env MVS_BENCH_PHASES and MVS_BENCH_SYNC (run) | instrumentation: emit phase boundaries through bench_timer.h |

### Harness deviations

| class         | affects               | reversible              | description                                                                                                                                                                                                                                                        |
|---------------|-----------------------|-------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| interface     | none                  |                         | Preprocessing is app_initialize_ETH3D rather than the shared converter, because CUMVS consumes a different input format entirely. Both do view selection and both are excluded from the measured region, which is comparable - but it must be stated, not assumed. |
| normalization | timing,memory,quality | flag --max-neighbors=20 | "norm10: --max-neighbors=10 passed to app_initialize_ETH3D (the shipped value is 20), the CUMVS-side equivalent of the shared converter's neighbour count."                                                                                                        |
| normalization | none                  |                         | R-EXP-11 (debug artefact output off) needs no action for this method: it writes no debug artefacts. The harness must NOT pass --no-debug-output to it -- the binary has no such flag.                                                                              |

## Deviations that apply to every method

| class           | status | affects               | reversible                                               | description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | evidence                                   |
|-----------------|--------|-----------------------|----------------------------------------------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|--------------------------------------------|
| build           | active | timing,memory,quality |                                                          | Docker toolchain. Base image nvidia/cuda:12.8.1-devel-ubuntu24.04; host and CUDA-host compiler clang-18 (never the distribution gcc); nvcc from /usr/local/cuda-12.8; CMAKE_BUILD_TYPE=Release for every method; Ninja generator. Eigen pinned to 3.4.0. OpenCV pinned to 4.12.0 with opencv_contrib, built static with BUILD_LIST=core,imgcodecs,imgproc,calib3d,cudev,highgui,viz, WITH_CUDA=ON, WITH_VTK=ON. PCL comes from Ubuntu's libpcl-dev and is linked by HPM-MVS only. None of the eleven methods was released against this toolchain, so no measurement here is a measurement of the authors' binaries.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | benchmark/Dockerfile                       |
| build           | active | timing,memory,quality | flag -DCMAKE_CUDA_ARCHITECTURES                          | CUDA architecture target. Passed to every method build as -DCMAKE_CUDA_ARCHITECTURES=<arch>. The campaign target is 120 (the Blackwell benchmark fleet); the local development GPU is 75. Every published number states which. `deviations verify --arch <arch> --image <image>` runs `cuobjdump -res-usage` on each built binary inside the image and requires the method's own kernels to carry SASS for exactly that architecture; the statically linked OpenCV CUDA modules (`cv::cudev::…`) ship SASS for sm_50–sm_120 regardless and are only checked for covering the target (D22). Upstream's ACMH, ACMM, ACMP, ACMMP, MP-MVS and DPE-MVS created their targets with FindCUDA's `cuda_add_executable`, which ignores CMAKE_CUDA_ARCHITECTURES and CMAKE_CUDA_FLAGS; every fork now builds through enable_language(CUDA) (one `build` commit each, see the forks' logs), so the flag is authoritative everywhere and the binary check is what keeps that claim honest over time.                                                                                                                                                                                                                                   | fork-audit.md § C.1                        |
| build           | active | timing,quality        |                                                          | Common nvcc flags. The ACM lineage ships `-O3 --use_fast_math --maxrregcount=128 --ptxas-options=-v` in CUDA_NVCC_FLAGS and the harness keeps them. --use_fast_math changes device floating-point semantics and --maxrregcount=128 is an occupancy control, so both are load-bearing for runtime and for the numbers. They must reach the actual compile path in every fork. The MP-MVS fork had moved them to CMAKE_CUDA_FLAGS while its target was still created with `cuda_add_executable`, so it was compiled with neither (fork-audit.md § B.8 #3c, § C.1); repaired by its `build: build CUDA through the CUDA language` commit, verified on the nvcc command line and with cuobjdump. No MP-MVS number predates the repair.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | fork-audit.md § C.1                        |
| normalization   | active | quality,timing        | configuration author                                     | In the `norm10` configuration one shared converter, `colmap2mvsnet_acm_perf`, replaces the shipped converter of every method and emits 10 source views instead of the shipped 20. View selection is byte-identical in all five shipped variants, so the substitution changes no neighbour ranking; it equalises the image bytes and intrinsics that variants D/E (APD-MVS, DVP-MVS, DPE-MVS) otherwise pad, re-encode and rescale. Applies to `norm10` only; `author` runs each fork's own converter (R-EXP-06). Padding under `norm10` is all-or-none per scene (R-EXP-09).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | param-survey.md § C; decisions D02, D18    |
| normalization   | active | timing,io             | flag --no-debug-output                                   | Debug artefact output disabled in every configuration behind a single `--no-debug-output` flag, with the upstream behaviour left as the binary's default. Upstream ships debug output ON in seven of the ten campaign methods: DPE-MVS's DEBUG_COMPLEX (a full-resolution D2H copy plus a JPEG encode per RunPatchMatch) and show_medium_result=true (3 JPEGs per image pass, plus 2 per edge pre-pass), APD-MVS's show_medium_result forced true in main (3 JPEGs per image pass), MP-MVS's four config.yaml switches plus an unconditional costs.jpg per image pass, HPM-MVS's depths_prior.dmb per prior pass, HPM-MVS++'s triangulation*.png and up to two depths_prior*.dmb per prior pass, and the triangulation.png of ACMP, ACMMP and MP-MVS. Every fork's default is the upstream behaviour, config.yaml and DPE's #define DEBUG_COMPLEX included; the flag only adds a way to skip the writes at run time. ACMH and ACMM write no debug artefacts and take no flag; CUMVS writes none unless -d is given and its OpenCV CommandLineParser rejects unknown keys, so the flag must NOT be passed to it. Per policy rule 1 both settings are measured in the pilot so the pair is a result, not just a disclosure. | fork-audit.md § B.8                        |
| instrumentation | active | timing                | flag BENCH_PHASES=0 at compile time; env var at run time | Phase-timer instrumentation. A single `bench_timer.h` vendored byte-identically into all ten campaign forks (md5 must match across them), emitting `PHASE <name> BEGIN\|END <monotonic_ns> [k=v ...]` for the canonical_phases above to the file named by MVS_BENCH_FILE, line buffered so a crashed or killed run keeps its partial trace. It is compiled out entirely under -DBENCH_PHASES=0 and, when compiled in, takes one cached always-false branch per boundary unless MVS_BENCH_PHASES is set; output never goes to the binary's own stdout or stderr. Every kernel launch in the nine non-CUMVS methods is already followed by cudaDeviceSynchronize, so every canonical phase boundary is a true sync point and no extra synchronisation is introduced. CUMVS has two unsynchronised launches (initializeDepthsAndNormals, calcInitialCosts); their cost is attributed to the following patchmatch.propagate span unless MVS_BENCH_SYNC is set, which inserts one cudaDeviceSynchronize at the end of patchmatch.init and must itself be measured as an on/off pair (R-TIM-08). Overhead must be measured (instrumented vs BENCH_PHASES=0 build), not assumed (R-TIM-09).                                      | phase-survey.md § D                        |
| interface       | active | none                  |                                                          | Dataset presentation. The harness binds an ETH3D scene into the container as three read-only mounts: `images/`, `dslr_calibration_undistorted/`, and the same calibration directory a second time as `sparse/`. The ACM-lineage converters (ACMH, ACMM, ACMP, ACMMP, HPM-MVS, HPM-MVS++, MP-MVS) read `<dense_folder>/sparse` as a COLMAP dense workspace would have it; APD-MVS, DPE-MVS and CUMVS read `dslr_calibration_undistorted` directly. Both see identical bytes; no fork's converter is edited and nothing is copied or rewritten. Image paths in images.txt carry the `dslr_images_undistorted/` prefix, so `images/<name>` resolves for both.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | benchmark/src/bench/runner.py scene_mounts |
