# Generated by bin/gen_dataset.py; reproduced bit-for-bit from the seed and
# parameters recorded in golden/dataset_*_manifest.json. 78 MB, not tracked.
data/
# Per-variant build trees and gate scratch.
build/
results/gate_*.json
# macOS `sample` call trees: 1-2 MB each, regenerate with bin/profile.sh.
results/profile_*.txt
results/*.log
.mvebench.lock
