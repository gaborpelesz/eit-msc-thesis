diff --git a/CMakeLists.txt b/CMakeLists.txt
index c81f9d8..e67edd0 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -1,7 +1,9 @@
 project(ETH3DMultiViewEvaluation)
 cmake_minimum_required(VERSION 2.8)
 
-set(CMAKE_CXX_STANDARD 17)
+set(CMAKE_CXX_STANDARD 20)
+set(CMAKE_CXX_STANDARD_REQUIRED ON)
+set(CMAKE_CXX_EXTENSIONS OFF)
 
 set(CMAKE_MODULE_PATH ${CMAKE_SOURCE_DIR}/cmake)
 
@@ -14,12 +16,6 @@ link_directories(${Boost_LIBRARY_DIRS})
 find_package(Eigen3 REQUIRED)
 include_directories(${EIGEN3_INCLUDE_DIRS})
 
-# External dependency: PCL
-find_package(PCL 1.7 REQUIRED)
-include_directories(${PCL_INCLUDE_DIRS})
-link_directories(${PCL_LIBRARY_DIRS})
-add_definitions(${PCL_DEFINITIONS})
-
 # OpenMP
 find_package(OpenMP)
 if (OPENMP_FOUND)
@@ -27,16 +23,18 @@ if (OPENMP_FOUND)
   set (CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} ${OpenMP_CXX_FLAGS}")
 endif()
 
+# External dependency: PCL
+find_package(PCL 1.14 REQUIRED)
+include_directories(${PCL_INCLUDE_DIRS})
+link_directories(${PCL_LIBRARY_DIRS})
+add_definitions(${PCL_DEFINITIONS})
+
 # Settings.
 if(NOT CMAKE_BUILD_TYPE)
   message(STATUS "Build type not specified, using RelWithDebInfo")
   set(CMAKE_BUILD_TYPE RelWithDebInfo)
 endif()
 
-if(NOT "${CMAKE_CXX_COMPILER_ID}" STREQUAL "MSVC")
-    add_definitions("-O2 -msse2 -msse3 -std=c++11")
-endif()
-
 include_directories(
     ./src
 )
